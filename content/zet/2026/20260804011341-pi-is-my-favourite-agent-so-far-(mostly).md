+++
title = "pi is my favourite agent so far (mostly)"
categories = ["zet"]
tags = ["zet"]
slug = "pi-is-my-favourite-agent-so-far-(mostly)"
date = "2026-08-04 00:00:00 +0000 UTC"
draft = "false"
ShowToc = "true"
mermaid = "true"
+++

# pi is my favourite agent so far (mostly)

I really like the claude harness. I think it writes pretty good code and I like to shape it as it writes in my IDE - rejecting bad stuff early.

But I reflect that I mostly do this because its soo **slow** that if I was to let it run and write a bunch of crap it would then take me ages to chat back and forth to fix it. 

A simple question is a 30 to 500s penalty with claude. 

Contrast that with `pi`. I highlight some text in my IDE, ask it a question and its responded sometimes faster than *I* can compute! Note, this is `gpt-5.6-luna` so its 
not `opus`/`sonnet` level. Maybe `haiku` would be the same but every time I use that model it stinks! `luna` is a boss!

So I've now adopted a 60/40 split. Claude is my architectural partner and helps verify - and writes a lot too. `pi` is my vanguard, hes got my back and lets me ask beacoup questions
without interrupting `claude` whilst he burns 5 gallons of water and two households of electricity just to answer a simple question.

Tags:

    #ai #pi #claude

